import React from 'react'

function SingleProduct() {

  // homePage.jsx peat SingeProduct URL peale saatumine jasaamta element.id
  //  App.js sees tee võimekus see vastu võtta

  // siin failis use params abil võta see id Url-st

  // otsi üles õige toode mitte: productsFromFile[id] productFromFile[]
  // ÕIGE lahenduse productsFromFile.find(e = e.id === Number(URLid)
  return (
    <div>
      
    </div>
  )
}

export default SingleProduct
